# =============================================================================
# Environment template — canonical file is one directory up:
#   ../.env.example   →  inventoryProjBrgy/inventoryProjBrgy/.env.example
#
# Copy into the app directory (same folder as dbcon.php):
#   cp ../.env.example ../.env.local
#   (Windows: copy ..\.env.example ..\.env.local)
#
# MySQL migrations do not read this file; they use the mysql client and your
# credentials. The template above is for PHP (app, API, worker).
# Never commit .env.local or real secrets.
# =============================================================================
